import 'package:flutter/material.dart' hide ModalBottomSheetRoute;

// Colors that we use in our app
const colorPrimary =  Color.fromRGBO(24, 119, 242, 1.0);
const colorAccent =  Color.fromARGB(255, 247, 107, 57) ;
const colorSelectedButton =  Color.fromRGBO(31, 177, 198, 1) ;
const colorShadowButton =  Color.fromRGBO(0, 0, 0, 0.08) ;
const colorText =  Color.fromRGBO(82, 92, 106, 1);
const colorOnPrimary =  Color.fromRGBO(244, 244, 246, 1);
